{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint 2>&1 && npm test 2>&1 || echo '{\"decision\":\"block\",\"reason\":\"Verification failed: lint or tests did not pass. Fix the issues before finishing.\"}'",
            "timeout": 120,
            "statusMessage": "Verifying (lint + tests)..."
          }
        ]
      }
    ]
  }
}
